{
  // Path replacement rules for GitHub Copilot AI developer-specific configurations.
  // Replaces local repository paths with anonymized tokens in session data.
  "rules": [
    {
      // GitHub Copilot session paths will be added here when GitHub Copilot is configured
      "paths": [],
      "replacement": "_www_"
    }
  ]
}
