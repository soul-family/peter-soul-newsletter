{
  // Path replacement rules for OpenCode AI developer-specific configurations.
  // Replaces local repository paths with anonymized tokens in session data.
  "rules": [
    {
      // OpenCode session paths will be added here when OpenCode is configured
      "paths": [],
      "replacement": "_www_"
    }
  ]
}
