# Todo - Ignore

Deprecated or intentionally excluded items. No T-numbers assigned. Do not action.

- Add compression or archival strategy for `.ai-activity/ai-sessions/*.db` files to reduce local storage footprint
- Add session database encryption option for sensitive data
- Add documentation search index generation
- Add gzip compression support for archived session databases
- Implement backup merge strategy for combining multiple backup databases
- Add unit tests for stats script normalization and user activity calculation
